#ifndef SLANG_PKG_STRINGS_H
#define SLANG_PKG_STRINGS_H

/* The package cannot be called `str`: that token is the type. `strings`
   follows Go's naming for the same reason and the same operations. */
#define PKG_STRINGS_NAME "strings"

#endif
