#ifndef SLANG_PKG_PROC_H
#define SLANG_PKG_PROC_H

/* The 'proc' package's import name, as recognized by loader.c. The
 * rest of proc's implementation lives in this directory: sigs.c
 * (NatSig table), runtime.c (embedded C runtime: signal handling,
 * getenv). */
#define PKG_PROC_NAME "proc"

#endif /* SLANG_PKG_PROC_H */
